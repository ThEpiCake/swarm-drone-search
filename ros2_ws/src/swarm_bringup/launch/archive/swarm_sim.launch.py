"""Top-level swarm simulation launch — M0 skeleton, expand each milestone."""
from launch import LaunchDescription


def generate_launch_description():
    return LaunchDescription([
        # TODO M1: spawn single drone + PX4 SITL + uXRCE-DDS bridge
        # TODO M2: parameterised multi-vehicle spawn
    ])
